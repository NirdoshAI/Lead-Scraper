{
  "framework": "vite",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/api-apify/(.*)",
      "destination": "https://api.apify.com/$1"
    }
  ]
}
